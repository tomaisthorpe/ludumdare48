local config = {
  skipSystem = false,
  fullscreen = false,
  borderColor = {9 / 255, 18 / 255, 26 / 255},

  miniPlanetRadius = 60,
  planetsPerSystem = 3,
  planetSize = {2000,1000},
  physicsDebug = true,
}

return config
